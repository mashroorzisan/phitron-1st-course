#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    long long int a, b;
    long long int mul;
    scanf("%lld %lld", &a, &b);
    mul = a * b;
    printf("%lld\n", mul);
    return 0;
}
